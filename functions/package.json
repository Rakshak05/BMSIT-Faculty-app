{
  "name": "bmsit-faculty-functions",
  "private": true,
  "engines": {
    "node": "^18"
  },
  "scripts": {
    "lint": "echo 'no lint'",
    "build": "echo 'no build'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.7.0",
    "@google-cloud/dialogflow": "^6.6.0",
    "chrono-node": "^2.7.6",
    "dotenv": "^16.4.0"
  }
}